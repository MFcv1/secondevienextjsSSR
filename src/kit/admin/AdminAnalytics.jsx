'use client';

import AdminAnalyticsOverview from './AdminAnalyticsOverview';

export default function AdminAnalytics({ darkMode = false }) {
  return <AdminAnalyticsOverview darkMode={darkMode} />;
}

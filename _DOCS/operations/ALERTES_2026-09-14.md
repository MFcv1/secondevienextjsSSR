# Alertes sandbox et contrôles CI — 14 septembre 2026

Demande : corriger les alertes reçues sur LOA et les échecs GitHub en préservant
les fonctionnalités, sans navigateur. Projet `secondevienextjsssr` uniquement.

## Corrections actives dans Google Cloud

- Le warning « G1 Sandbox - Cloud Tasks backlog » exclut les douze files de
  maintenance à échéance définies dans `scripts/lib/deferred-queue-alerts.mjs`.
  Les files de travail immédiat restent surveillées à profondeur > 0 sur 5 min.
- Les cinq protections transport couvrent création refusée, livraison refusée,
  retard effectif p99 > 5 min, événement non acquitté > 10 min et queue
  suspendue/supprimée/purgée. La règle de retard manquante a été créée.
  Le canal e-mail existant de LOA et le canal Pub/Sub sont présents sur chacune.
  Elles ont été appliquées et relues avant la modification du backlog.
- `dispatchCatalogBuild` : `dispatchcatalogbuild-00018-keb`, ACTIVE.
  Une construction dépassée avant CAS confirme son successeur puis termine en
  `superseded`. Après CAS, une nouvelle mutation ne bloque plus l'enregistrement
  du fait publié ; lease, expiration, rollback et protection contre un retour
  à une révision plus ancienne restent contrôlés.
- `dispatchCatalogRevalidation` : `dispatchcatalogrevalidation-00017-vaf`,
  ACTIVE. Quatre essais HTML avec 10 secondes d'attente cumulée permettent à
  Next de régénérer les pages. Une preuve encore ancienne reste en échec et
  suit le mécanisme de reprise durable existant.

Chaque déploiement Functions reprend son archive cloud exacte : seul le module
corrigé, `package.json` et `package-lock.json` diffèrent. Aucun autre code métier
n'a été livré. Configuration runtime, secrets référencés, IAM et paramètres des
deux queues comparés avant/après : identiques. Aucune tâche purgée ou forcée.
Le 14 septembre à 15:04 UTC, `/` répond 200, API version à révision 374 et
marqueur HTML identique. Aucune mutation de meuble/stock/commande pour ce contrôle.

## Corrections locales pour GitHub

Le test d'inventaire API couvre désormais les cinq routes ajoutées, avec leurs
contrôles Auth, App Check, bornage ou désactivation par défaut. Les suites
transport passkey/opérations publiques restent exercées.

La suite Functions ignorait les `Object.assign` de maintenance et comptait
des helpers comme des endpoints. L'inventaire statique recense désormais
186 exports de fonctions et sa parité est vérifiée avec le module d'entrée,
sans invoquer les handlers. Les deux façades CloudEvent sans métadonnées
Firebase sont conservées ; trois helpers internes sont exclus du comptage.
Les 181 fonctions cloud ont été relues ; les deux candidats de publication
restent locaux. Les assertions de photographies historiques OTP sont corrigées
d'après leurs manifestes (162), sans modifier ces photographies.

Après le premier test réparé, `security:audit` révélait aussi des dépendances
vulnérables. Versions correctives verrouillées : Next/plugin ESLint 16.3.3,
SimpleWebAuthn serveur 13.3.2, Nodemailer 9.1.1, Sharp 0.35.4 (y compris sous
Next), csv-parse 7.0.2, Hono 4.13.5, js-yaml 4.3.2 et Morgan 1.12.0.
Les trois dépendances Functions mises à jour sont incluses dans les deux
déploiements ciblés ; aucun déploiement global Functions. Next et les dépendances
Hosting sont également livrés dans le build sandbox `019`, avec le correctif
d'ajout au panier demandé ensuite.

Références : [métriques Cloud Tasks](https://docs.cloud.google.com/tasks/docs/monitor),
[correctif Next](https://github.com/vercel/next.js/releases/tag/v16.3.3),
[Nodemailer 9.1.1](https://github.com/nodemailer/nodemailer/releases/tag/v9.1.1).

## Vérifications et limites

- Node 22.23.2, pnpm 11.7.0 ; 143 tests ciblés et 91 tests Auth réussis.
- Contrats Functions : 167 tests réussis ; release/cache : 9 tests réussis.
- Audit de dépendances racine et Functions : zéro vulnérabilité connue.
- Build Next 16.3.3 avec fixture locale réussi ; routes publiques ISR/SSG
  conservées. Cette fixture n'est pas déployée.
- Lint ciblé et `git diff --check` ; aucun navigateur, Playwright ou E2E.
- Corrections alertes/CI enregistrées dans `588543c`. Hosting `019` est
  READY/SUCCEEDED à 100 %, identifiant `sv-mu1dz3bq-83f2f68bd80d` relu sur
  quatre routes HTTP 200 ; contrôles SSR publics réussis. Aucun push ni
  relancement GitHub : la CI distante complète n'est pas déclarée validée.
  Les anciens mails ne sont pas supprimés et un nouvel incident réel doit
  toujours produire une alerte.
- Pas d'injection de panne cloud ni de nouvelle publication métier : les
  scénarios de concurrence/reprise sont prouvés localement, la livraison et
  l'état public sont vérifiés séparément. Cela ne garantit pas zéro incident futur.

## Complément demandé : ajout au panier sans ouverture automatique

Les deux appels d'ouverture après ajout (invité/connecté) sont retirés ; le
chargement différé ne montre son écran que pour `sv:open-cart`. La sauvegarde,
les compteurs, le rejeu des ajouts, les confirmations et le checkout sont
inchangés. 23 tests panier/handoff/reprise/release réussis, lint ciblé et builds
local/cloud réussis. Aucun navigateur/E2E ni ajout réel de meuble effectué.

L'archive Hosting `019` compte 1 247 fichiers identiques au local, sans fichier
d'environnement privé ni build fixture. `/`, `/galerie`, `/wishlist`, `/checkout`
répondent 200 avec le nouvel identifiant ; contrôles SSR accueil, à-propos,
produit et catégorie réussis. Preuves `hosting-cart-*` dans le dossier ci-dessous.
Rollback Hosting : `build-2026-09-14-018` (source Git `d3368ec`), actif à 100 %
avant cette livraison. Aucun nouveau déploiement Functions pour ce complément.

Preuves privées ignorées : `logs/alert-repair-20260914/`, dont configurations
avant/après, `source-proof.json`, tests, builds et `verification.json`.
Rollback ciblé : redéployer la source `build-original/` ou
`revalidation-original/` en conservant la configuration correspondante.
Pour Monitoring, restaurer les règles sauvegardées dans
`monitoring-applied/before.json` ; la nouvelle règle de retard n'existait pas
dans cette photographie. Ne pas désactiver les protections transport pour
faire disparaître une alerte réelle.
